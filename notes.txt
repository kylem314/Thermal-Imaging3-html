roi = region of interest

Javascript example data passage
  var username = {{ data.username }}
  var site = {{ data.site }}



y.jpg = first frame of video

VERSIONS

v1 = only shows/plays video
v2 = shows video, and change coord variables to change the blue rectangle on it
v3 = shows video with blue rectangle (change coord values if necessary). Averages grayscale value of region and returns in console